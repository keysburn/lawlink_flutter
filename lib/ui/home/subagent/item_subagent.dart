import 'package:flutter/material.dart';

class SubagentItemWidget extends StatelessWidget {
  const SubagentItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
